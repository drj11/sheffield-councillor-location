#!/bin/sh
LC_ALL=C export LC_ALL
sort -t'	' -k 5 data/subject.tsv |
  join -t'	' -1 5 - data/map-postcode-latlon.tsv  |
  sort -t'	' -k 2 |
  join -t'	' -1 2 - data/map-ward-latlon.tsv |
  sort -t'	' -k 2 |
  join -t'	' -1 2 - data/map-postcode-ward.tsv
