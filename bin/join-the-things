#!/bin/sh
LC_ALL=C export LC_ALL
sort -t'	' -k 5 data/addr.tsv |
  join -t'	' -1 5 - data/latlon.tsv  |
  sort -t'	' -k 2 |
  join -t'	' -1 2 - data/ward-points.tsv |
  sort -t'	' -k 2 |
  join -t'	' -1 2 - data/postcode-ward.tsv
